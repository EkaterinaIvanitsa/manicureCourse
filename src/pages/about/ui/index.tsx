import { Header } from "@/widgets";
import { FC } from "react";

export const About: FC = () => {
  return (
    <>
      <section
        style={{
          backgroundColor: "#97aac2",
          height: "100vh",
        }}
      >
        <Header />
      </section>
    </>
  );
};
